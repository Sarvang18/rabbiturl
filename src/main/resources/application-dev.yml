spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.autorabit.rabbiturl: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
    org.apache.kafka: WARN
